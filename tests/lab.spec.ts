/*
let edad: number = 25;
let altura: number = 174.5;

//operaciones numericas
let suma: number = edad + 10;
let promedio: number = (edad + altura) / 2;

//strings
let nombres: string = "Jorge Dioney";
let apellidos: string = "Martínez Vázquez";

//cncatenación
let nombreCompleto: string = nombres + " " + apellidos;

//boleanos
let online: boolean = true;
let offline: boolean = false;

//condicionales
if (online) {
    console.log("El usuario " + nombreCompleto + " está online")
} else {
    console.log("El usuario " + nombreCompleto + " está offline")
}

//arreglos
let numeros: number[] = [1, 2, 3, 4, 5] //arreglo de numeros
let frutas: string[] = ["manzana", "platano", "naranja"] //arreglo de strings

console.log(numeros[2]) // acceso al tercer elemento (índice 2)
console.log(frutas.length) //longitud del array

let persona: [string, number] = ["Alice", 99]; // Tupla de nombre y edad
let coordenadas: [number, number] = [45.6, -73.8] //tupla de coordenadas

console.log(persona[0]) //acceso al nombre
console.log(coordenadas[1]) //acceso a la longitud

enum Color { Rojo, Verde, Azul }

let colorElegido: Color = Color.Verde;
if(colorElegido === Color.Verde){
    console.log("El color elegido es el verde")
}

let nombreInferido = "Alice" //TypeScript infiere que "nombreInferido" es de tipo string
let edadInferida = 25;      //TypeScript infiere que "edadInferida" es de tipo number
*/
